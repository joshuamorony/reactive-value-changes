import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'reactive-values-changes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
